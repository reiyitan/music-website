import React from "react"; 

const Playlist = (props) => {
    return (
        <div>
            <button className="sidebar-playlist-button">placeholder</button>
        </div>
    );
}

export default Playlist;